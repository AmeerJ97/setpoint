node_modules/
*.log
.claude/
.claude.json
.research/
.research/*
AGENTS.md
docs/superpowers/
docs/assets/brand/render.html
docs/assets/brand/hero.png
tools/
research/*.md
research/claude_advisor.py
research/mitm_claude_override.py
!research/train-advisor-classifier.py

# Rust guard build artifacts — binary is built locally via scripts/build-guard.sh
src/guard/rust/target/

# Research debris — research/ itself is tracked as product input; exclude
# superseded variants and scratch files so they don't leak into commits.
research/*-old.py
research/**/*.tmp

# Python test cache
__pycache__/
.pytest_cache/

# User-trained advisor classifier weights (generated by
# research/train-advisor-classifier.py). The committed
# classifier-weights.default.json is the fallback.
src/advisor/classifier-weights.json
.gitnexus
